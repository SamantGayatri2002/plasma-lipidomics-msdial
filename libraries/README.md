# MS-DIAL Libraries

Large MS-DIAL spectral libraries are intentionally excluded from this repository.

The workflow uses external MS-DIAL lipid spectral libraries for MS/MS-based lipid annotation.

Library paths are specified in the parameter files.

Large spectral library files are excluded using `.gitignore`.
